/**
 * Sales Activities — Activity feed and tracking
 * Wired to: integratewise-spine-v2 Worker (live)
 */
import { SalesTodayView } from "@/components/views/sales/today-view";

export default SalesTodayView;
