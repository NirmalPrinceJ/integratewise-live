/**
 * Sales Dashboard — wired to integratewise-spine-v2 Worker (live)
 * Re-exports the full SalesOps dashboard with pipeline kanban, activity feed, rep leaderboard.
 */
import SalesOpsDashboard from "./salesops-dashboard";

export default SalesOpsDashboard;
