/**
 * Website Media — Media asset management
 * Wired to: integratewise-store Worker (live, R2 storage)
 */
import { ContentLibraryView } from "@/components/views/content-library-view";

export default ContentLibraryView;
